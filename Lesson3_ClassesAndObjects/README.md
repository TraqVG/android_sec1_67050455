# Lesson 3 - Classes and Objects

Kotlin console lab covering classes and object instances, primary/secondary constructors, properties, inheritance, interfaces and delegation, data classes, enums, singleton objects, destructuring, pairs/collections, and extension functions.

Run `Main.kt` from IntelliJ IDEA (or compile all files in `src/` with Kotlin/JVM).
